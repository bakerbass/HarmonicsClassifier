name: harmonics_classifier
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  # Core dependencies
  - pandas>=1.5.0
  - numpy>=1.23.0
  # PyTorch with CUDA 11.8
  - pytorch>=2.0.0
  - pytorch-cuda=11.8
  - torchvision
  - torchaudio
  # Audio processing
  - librosa>=0.10.0
  # Progress bar
  - tqdm>=4.65.0
  # Optional: For parquet support
  - pyarrow>=10.0.0
  # Data analysis and visualization
  - matplotlib>=3.7.0
  - seaborn>=0.12.0
  - scikit-learn>=1.3.0
  - tabulate>=0.9.0
  - pip
  - pip:
    - soundfile>=0.12.0
